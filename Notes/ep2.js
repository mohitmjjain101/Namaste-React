
/** npm install -D <dep_name>
 *  Here -D means dev dependency
 */

/** 
 *  whats-the-difference-between-tilde-and-caret-in-package-json
 * 
 * 
 *  "devDependencies": {
    "parcel": "^2.8.3"
    }
 * 
 *  "devDependencies": {
    "parcel": "~2.8.3"
    }

    6037

    ~version “Approximately equivalent to version”, will update you to all future patch versions, without incrementing the minor version. ~1.2.3 will use releases from 1.2.3 to <1.3.0.

    ^version “Compatible with version”, will update you to all future minor/patch versions, without incrementing the major version. ^1.2.3 will use releases from 1.2.3 to <2.0.0.
    
 */

/**
 *  Package.json are kind of configuration and node_modules are kind of database who contains all the packages.
 * 
 */

/**
 *  what is transitove dependency
 * 
 *  Package which is depeding on others are called transitive dependecy
 */

/**
 * Fetching react from CDN in HTML files os mot good way because it is costly operation.
 * Betting to manage dependecy through package.json
 * 
 * 
 */